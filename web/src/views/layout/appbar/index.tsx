import { Box } from '@mui/material';
import React from 'react';


function AppBar() {
  return (
    <Box sx={{position: "fixed", backgroundColor: "red"}}>
      AppBar
    </Box>
  );
}

export default AppBar;